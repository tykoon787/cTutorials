#include <stdio.h>
#include "list.h"
#include <stdlib.h>

int main(void)
{
	node_t *head;
	node_t *first = NULL;
	node_t *second = NULL;
	node_t *third = NULL;
	
	/* ALLOCATE MEMORY */
	first = malloc(sizeof(node_t));
	second = malloc(sizeof(node_t));
	third = malloc(sizeof(node_t));
	return (0);
}
